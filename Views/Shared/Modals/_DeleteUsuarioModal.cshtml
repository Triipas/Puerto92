@* Partial View: Modal para Eliminar Usuario *@

<div id="deleteModal" class="modal-overlay modal-delete" style="display: none;">
    <div class="modal-container">
        <div class="modal-header">
            <div>
                <h2 class="modal-title">
                    <i class="fa-solid fa-triangle-exclamation"></i>
                    Eliminar Usuario
                </h2>
                <p class="modal-subtitle">Esta acción es permanente y no se puede deshacer</p>
            </div>
            <button class="modal-close" onclick="closeModal('deleteModal')">&times;</button>
        </div>
        <div class="modal-body">
            <p style="margin-bottom: 1rem; color: #374151;">Está por eliminar al usuario:</p>
            
            <div class="delete-info-box">
                <div class="delete-info-row">
                    <span class="delete-info-label">Nombre:</span>
                    <span id="deleteNombre">-</span>
                </div>
                <div class="delete-info-row">
                    <span class="delete-info-label">Usuario:</span>
                    <span id="deleteUsuario">-</span>
                </div>
                <div class="delete-info-row">
                    <span class="delete-info-label">Rol:</span>
                    <span id="deleteRol">-</span>
                </div>
                <div class="delete-info-row">
                    <span class="delete-info-label">Local:</span>
                    <span id="deleteLocal">-</span>
                </div>
            </div>

            <div class="delete-warning">
                <div class="delete-warning-title">
                    <i class="fa-solid fa-xmark"></i>
                    Se eliminará:
                </div>
                <ul class="delete-warning-list">
                    <li>Acceso al sistema</li>
                    <li>Datos del usuario</li>
                    <li>Credenciales de autenticación</li>
                </ul>
            </div>

            <div class="delete-note">
                <div class="delete-note-title">
                    <i class="fa-solid fa-info-circle"></i>
                    Se mantendrán (para trazabilidad):
                </div>
                <ul class="delete-note-list">
                    <li>Kardex históricos con nombre visible</li>
                    <li>Movimientos realizados</li>
                    <li>Descuentos aplicados en reportes de RRHH</li>
                    <li>Asignaciones pasadas</li>
                    <li>Registros en "Personal Presente"</li>
                    <li>Historial de auditoría</li>
                </ul>
            </div>

            <div class="delete-security-note">
                <strong>Nota de seguridad:</strong> El usuario eliminado no podrá volver a iniciar sesión. Si intenta acceder, recibirá un mensaje genérico de error sin revelar detalles de la cuenta.
            </div>
        </div>
        <form id="deleteForm" method="post">
            <input type="hidden" id="deleteId" name="id" />
            <div class="modal-footer">
                <button type="button" class="btn btn-cancel" onclick="closeModal('deleteModal')">Cancelar</button>
                <button type="submit" class="btn btn-delete">
                    <i class="fa-solid fa-trash"></i>
                    Eliminar Definitivamente
                </button>
            </div>
        </form>
    </div>
</div>