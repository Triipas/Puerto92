@* Partial View: Modal para Crear Usuario *@

<div id="createModal" class="modal-overlay" style="display: none;">
    <div class="modal-container">
        <div class="modal-header">
            <div>
                <h2 class="modal-title">Crear Nuevo Usuario</h2>
                <p class="modal-subtitle">Complete los campos para crear un usuario en el sistema</p>
            </div>
            <button class="modal-close" onclick="closeModal('createModal')">&times;</button>
        </div>
        <form id="createForm" asp-action="Create" asp-controller="Usuarios" method="post">
            <div class="modal-body">
                <div class="form-group">
                    <label class="form-label">Nombre Completo <span style="color: red;">*</span></label>
                    <input type="text" name="NombreCompleto" class="form-control" placeholder="Ej: Juan Pérez García" required />
                </div>

                <div class="form-group">
                    <label class="form-label">Nombre de Usuario <span style="color: red;">*</span></label>
                    <input type="text" name="UserName" class="form-control" placeholder="Ej: juan.perez" required />
                </div>

                <div class="form-group">
                    <label class="form-label">Rol <span style="color: red;">*</span></label>
                    <select name="RolId" class="form-select" id="createRolId" required>
                        <option value="">Seleccione un rol</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Local <span style="color: red;">*</span></label>
                    <select name="LocalId" class="form-select" id="createLocalId" required>
                        <option value="">Seleccione un local</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="form-label">Email</label>
                    <input type="email" name="Email" class="form-control" placeholder="Ej: usuario@puerto92.com" />
                </div>

                <div class="form-group">
                    <label class="form-label">Teléfono</label>
                    <input type="text" name="Telefono" class="form-control" placeholder="Ej: +51 987654321" />
                </div>

                <div class="form-group">
                    <label class="form-label">Estado <span style="color: red;">*</span></label>
                    <select name="Activo" class="form-select">
                        <option value="true" selected>Activo</option>
                        <option value="false">Inactivo</option>
                    </select>
                </div>

                <div class="password-info">
                    <i class="fa-solid fa-info-circle"></i>
                    <span>Se generará automáticamente una contraseña temporal que deberá ser cambiada en el primer inicio de sesión.</span>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-cancel" onclick="closeModal('createModal')">Cancelar</button>
                <button type="submit" class="btn btn-primary">
                    <i class="fa-solid fa-plus"></i>
                    Crear Usuario
                </button>
            </div>
        </form>
    </div>
</div>