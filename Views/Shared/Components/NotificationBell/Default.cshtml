@model int

<div class="notification-bell" id="notificationBell">
    <button type="button" class="bell-button" onclick="toggleNotificationPanel()">
        <i class="fa-solid fa-bell"></i>
        @if (Model > 0)
        {
            <span class="notification-badge">@(Model > 99 ? "99+" : Model.ToString())</span>
        }
    </button>

    <!-- Panel de Notificaciones -->
    <div class="notification-panel" id="notificationPanel" style="display: none;">
        <div class="notification-header">
            <h3>Notificaciones</h3>
            <button type="button" class="mark-all-read" onclick="marcarTodasComoLeidas()">
                <i class="fa-solid fa-check-double"></i>
                Marcar todas como leídas
            </button>
        </div>

        <div class="notification-list" id="notificationList">
            <div class="notification-loading">
                <i class="fa-solid fa-spinner fa-spin"></i>
                <span>Cargando notificaciones...</span>
            </div>
        </div>

        <div class="notification-footer">
            <a href="#" onclick="verTodasLasNotificaciones(); return false;">
                Ver todas las notificaciones
            </a>
        </div>
    </div>
</div>